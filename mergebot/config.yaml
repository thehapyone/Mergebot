# config.yaml

llm:
  model: "azure/gpt-4.1"

repository:
  type: "gitlab"

  gitlab:
    url: "https://gitlab.its.getingecloud.net"
    project: "dts/lestrade/aide-recorder"
    base_branch: "main"

crews:
  # CodeAnalysis:
  #   llm:
  #     model: "azure/o3-mini"
  # ImpactEvaluator:
  #   llm:
  #     model: "azure/o3-mini"
  TestAnalysis:
    llm:
      model: "azure/gpt-4.1-mini"
