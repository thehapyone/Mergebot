# config.yaml

llm:
  model: "azure/gpt-4o-mini"

repository:
  type: "gitlab"

  gitlab:
    url: "https://gitlab.example.com/api/v4"
    private_token: "your-gitlab-private-token"
    project: "dts/lestrade/aide-recorder"
    base_branch: "main"

crews:
  CodeAnalysis:
    llm:
      model: "groq/llama-3.1-8b-instant"
  RiskAnalysis:
    llm:
      model: "gpt-3.5-turbo"
